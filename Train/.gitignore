# Python
__pycache__/
.venv/

# IDE
.idea/

# Generated training data (regenerated by 00_init and 09_git_training)
script/fix_wrong_explanation.jsonl
script/fix_wrong_fix.jsonl
script/fix_explanation_fix.jsonl
script/git_applications_pairs.jsonl
script/git_examples_pairs.jsonl
script/application_prompt_pairs.jsonl
script/comment_pairs.jsonl

# Notebook run outputs
script/run/

# Files written by generated ARO programs that escaped into the repository
# (GitLab #804). Every generated program now runs with its own working
# directory, HOME and TMPDIR, so nothing should land here again — these lines
# are the belt to that fix's braces, and cover the checkouts where the stray
# files from the September run are still sitting untracked.
script/app.log
script/test.txt
script/encoded.txt
script/decoded.txt
script/events.jsonl

# Pipeline data (regenerated by the pipeline)
data/
models/
release/

# The experiment store stays out of git — it is binary, it does not diff, and
# a review cannot read it. The committed record is the CSV exported from it
# into Train/runs/<release>/ (GitLab #812):
#   python3 Train/script/experiment_db.py --export --release <release>
experiments.db
experiments.db-journal
experiments.db-wal
