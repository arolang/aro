{"instruction": "Translate this Python to ARO:\n```python\ndef load_config():\n    data = open(\"config.json\").read()\n    record = parse(data)\n    return record[\"port\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['port']` becomes `Extract the <port> from the <record: port>` then `Return an <OK: status> with <port>`.\n</think>\n\n```aro\n(LoadConfig: config API) {\n    Read the <data> from the <file: \"config.json\">.\n    Extract the <record> from the <data>.\n    Extract the <port> from the <record: port>.\n    Return an <OK: status> with <port>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_config():\n    data = open(\"config.json\").read()\n    record = parse(data)\n    return record[\"port\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['port']` becomes `Extract the <port> from the <record: port>` then `Return an <OK: status> with <port>`.\n</think>\n\n```aro\n(LoadConfig: config API) {\n    Read the <data> from the <file: \"config.json\">.\n    Extract the <record> from the <data>.\n    Extract the <port> from the <record: port>.\n    Return an <OK: status> with <port>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_config():\n    data = open(\"config.json\").read()\n    record = parse(data)\n    return record[\"port\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['port']` becomes `Extract the <port> from the <record: port>` then `Return an <OK: status> with <port>`.\n</think>\n\n```aro\n(LoadConfig: config API) {\n    Read the <data> from the <file: \"config.json\">.\n    Extract the <record> from the <data>.\n    Extract the <port> from the <record: port>.\n    Return an <OK: status> with <port>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_profile():\n    data = open(\"profile.json\").read()\n    record = parse(data)\n    return record[\"email\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['email']` becomes `Extract the <email> from the <record: email>` then `Return an <OK: status> with <email>`.\n</think>\n\n```aro\n(LoadProfile: profile API) {\n    Read the <data> from the <file: \"profile.json\">.\n    Extract the <record> from the <data>.\n    Extract the <email> from the <record: email>.\n    Return an <OK: status> with <email>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_profile():\n    data = open(\"profile.json\").read()\n    record = parse(data)\n    return record[\"email\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['email']` becomes `Extract the <email> from the <record: email>` then `Return an <OK: status> with <email>`.\n</think>\n\n```aro\n(LoadProfile: profile API) {\n    Read the <data> from the <file: \"profile.json\">.\n    Extract the <record> from the <data>.\n    Extract the <email> from the <record: email>.\n    Return an <OK: status> with <email>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_profile():\n    data = open(\"profile.json\").read()\n    record = parse(data)\n    return record[\"email\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['email']` becomes `Extract the <email> from the <record: email>` then `Return an <OK: status> with <email>`.\n</think>\n\n```aro\n(LoadProfile: profile API) {\n    Read the <data> from the <file: \"profile.json\">.\n    Extract the <record> from the <data>.\n    Extract the <email> from the <record: email>.\n    Return an <OK: status> with <email>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_order():\n    data = open(\"order.json\").read()\n    record = parse(data)\n    return record[\"total\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['total']` becomes `Extract the <total> from the <record: total>` then `Return an <OK: status> with <total>`.\n</think>\n\n```aro\n(LoadOrder: order API) {\n    Read the <data> from the <file: \"order.json\">.\n    Extract the <record> from the <data>.\n    Extract the <total> from the <record: total>.\n    Return an <OK: status> with <total>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_order():\n    data = open(\"order.json\").read()\n    record = parse(data)\n    return record[\"total\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['total']` becomes `Extract the <total> from the <record: total>` then `Return an <OK: status> with <total>`.\n</think>\n\n```aro\n(LoadOrder: order API) {\n    Read the <data> from the <file: \"order.json\">.\n    Extract the <record> from the <data>.\n    Extract the <total> from the <record: total>.\n    Return an <OK: status> with <total>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_order():\n    data = open(\"order.json\").read()\n    record = parse(data)\n    return record[\"total\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['total']` becomes `Extract the <total> from the <record: total>` then `Return an <OK: status> with <total>`.\n</think>\n\n```aro\n(LoadOrder: order API) {\n    Read the <data> from the <file: \"order.json\">.\n    Extract the <record> from the <data>.\n    Extract the <total> from the <record: total>.\n    Return an <OK: status> with <total>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_settings():\n    data = open(\"settings.yaml\").read()\n    record = parse(data)\n    return record[\"theme\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['theme']` becomes `Extract the <theme> from the <record: theme>` then `Return an <OK: status> with <theme>`.\n</think>\n\n```aro\n(LoadSettings: settings API) {\n    Read the <data> from the <file: \"settings.yaml\">.\n    Extract the <record> from the <data>.\n    Extract the <theme> from the <record: theme>.\n    Return an <OK: status> with <theme>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_settings():\n    data = open(\"settings.yaml\").read()\n    record = parse(data)\n    return record[\"theme\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['theme']` becomes `Extract the <theme> from the <record: theme>` then `Return an <OK: status> with <theme>`.\n</think>\n\n```aro\n(LoadSettings: settings API) {\n    Read the <data> from the <file: \"settings.yaml\">.\n    Extract the <record> from the <data>.\n    Extract the <theme> from the <record: theme>.\n    Return an <OK: status> with <theme>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_settings():\n    data = open(\"settings.yaml\").read()\n    record = parse(data)\n    return record[\"theme\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['theme']` becomes `Extract the <theme> from the <record: theme>` then `Return an <OK: status> with <theme>`.\n</think>\n\n```aro\n(LoadSettings: settings API) {\n    Read the <data> from the <file: \"settings.yaml\">.\n    Extract the <record> from the <data>.\n    Extract the <theme> from the <record: theme>.\n    Return an <OK: status> with <theme>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_invoice():\n    data = open(\"invoice.json\").read()\n    record = parse(data)\n    return record[\"amount\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['amount']` becomes `Extract the <amount> from the <record: amount>` then `Return an <OK: status> with <amount>`.\n</think>\n\n```aro\n(LoadInvoice: invoice API) {\n    Read the <data> from the <file: \"invoice.json\">.\n    Extract the <record> from the <data>.\n    Extract the <amount> from the <record: amount>.\n    Return an <OK: status> with <amount>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_invoice():\n    data = open(\"invoice.json\").read()\n    record = parse(data)\n    return record[\"amount\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['amount']` becomes `Extract the <amount> from the <record: amount>` then `Return an <OK: status> with <amount>`.\n</think>\n\n```aro\n(LoadInvoice: invoice API) {\n    Read the <data> from the <file: \"invoice.json\">.\n    Extract the <record> from the <data>.\n    Extract the <amount> from the <record: amount>.\n    Return an <OK: status> with <amount>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_invoice():\n    data = open(\"invoice.json\").read()\n    record = parse(data)\n    return record[\"amount\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['amount']` becomes `Extract the <amount> from the <record: amount>` then `Return an <OK: status> with <amount>`.\n</think>\n\n```aro\n(LoadInvoice: invoice API) {\n    Read the <data> from the <file: \"invoice.json\">.\n    Extract the <record> from the <data>.\n    Extract the <amount> from the <record: amount>.\n    Return an <OK: status> with <amount>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_device():\n    data = open(\"device.json\").read()\n    record = parse(data)\n    return record[\"serial\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['serial']` becomes `Extract the <serial> from the <record: serial>` then `Return an <OK: status> with <serial>`.\n</think>\n\n```aro\n(LoadDevice: device API) {\n    Read the <data> from the <file: \"device.json\">.\n    Extract the <record> from the <data>.\n    Extract the <serial> from the <record: serial>.\n    Return an <OK: status> with <serial>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_device():\n    data = open(\"device.json\").read()\n    record = parse(data)\n    return record[\"serial\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['serial']` becomes `Extract the <serial> from the <record: serial>` then `Return an <OK: status> with <serial>`.\n</think>\n\n```aro\n(LoadDevice: device API) {\n    Read the <data> from the <file: \"device.json\">.\n    Extract the <record> from the <data>.\n    Extract the <serial> from the <record: serial>.\n    Return an <OK: status> with <serial>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_device():\n    data = open(\"device.json\").read()\n    record = parse(data)\n    return record[\"serial\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['serial']` becomes `Extract the <serial> from the <record: serial>` then `Return an <OK: status> with <serial>`.\n</think>\n\n```aro\n(LoadDevice: device API) {\n    Read the <data> from the <file: \"device.json\">.\n    Extract the <record> from the <data>.\n    Extract the <serial> from the <record: serial>.\n    Return an <OK: status> with <serial>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_session():\n    data = open(\"session.json\").read()\n    record = parse(data)\n    return record[\"token\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['token']` becomes `Extract the <token> from the <record: token>` then `Return an <OK: status> with <token>`.\n</think>\n\n```aro\n(LoadSession: session API) {\n    Read the <data> from the <file: \"session.json\">.\n    Extract the <record> from the <data>.\n    Extract the <token> from the <record: token>.\n    Return an <OK: status> with <token>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_session():\n    data = open(\"session.json\").read()\n    record = parse(data)\n    return record[\"token\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['token']` becomes `Extract the <token> from the <record: token>` then `Return an <OK: status> with <token>`.\n</think>\n\n```aro\n(LoadSession: session API) {\n    Read the <data> from the <file: \"session.json\">.\n    Extract the <record> from the <data>.\n    Extract the <token> from the <record: token>.\n    Return an <OK: status> with <token>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_session():\n    data = open(\"session.json\").read()\n    record = parse(data)\n    return record[\"token\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['token']` becomes `Extract the <token> from the <record: token>` then `Return an <OK: status> with <token>`.\n</think>\n\n```aro\n(LoadSession: session API) {\n    Read the <data> from the <file: \"session.json\">.\n    Extract the <record> from the <data>.\n    Extract the <token> from the <record: token>.\n    Return an <OK: status> with <token>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef load_report():\n    data = open(\"report.json\").read()\n    record = parse(data)\n    return record[\"summary\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['summary']` becomes `Extract the <summary> from the <record: summary>` then `Return an <OK: status> with <summary>`.\n</think>\n\n```aro\n(LoadReport: report API) {\n    Read the <data> from the <file: \"report.json\">.\n    Extract the <record> from the <data>.\n    Extract the <summary> from the <record: summary>.\n    Return an <OK: status> with <summary>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef load_report():\n    data = open(\"report.json\").read()\n    record = parse(data)\n    return record[\"summary\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['summary']` becomes `Extract the <summary> from the <record: summary>` then `Return an <OK: status> with <summary>`.\n</think>\n\n```aro\n(LoadReport: report API) {\n    Read the <data> from the <file: \"report.json\">.\n    Extract the <record> from the <data>.\n    Extract the <summary> from the <record: summary>.\n    Return an <OK: status> with <summary>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef load_report():\n    data = open(\"report.json\").read()\n    record = parse(data)\n    return record[\"summary\"]\n```", "output": "<think>\nThe Python opens and reads a file \u2192 ARO `Read ... from the <file>`. `parse(data)` extracts a record \u2192 `Extract the <record>`. Returning `record['summary']` becomes `Extract the <summary> from the <record: summary>` then `Return an <OK: status> with <summary>`.\n</think>\n\n```aro\n(LoadReport: report API) {\n    Read the <data> from the <file: \"report.json\">.\n    Extract the <record> from the <data>.\n    Extract the <summary> from the <record: summary>.\n    Return an <OK: status> with <summary>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "read", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getWeather() {\n  const res = await fetch(\"https://api.weather.com/v1\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetWeather: weather API) {\n    Fetch the <response> from the <url: \"https://api.weather.com/v1\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getWeather() {\n  const res = await fetch(\"https://api.weather.com/v1\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetWeather: weather API) {\n    Fetch the <response> from the <url: \"https://api.weather.com/v1\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getWeather() {\n  const res = await fetch(\"https://api.weather.com/v1\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetWeather: weather API) {\n    Fetch the <response> from the <url: \"https://api.weather.com/v1\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getRates() {\n  const res = await fetch(\"https://api.fx.com/latest\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetRates: rates API) {\n    Fetch the <response> from the <url: \"https://api.fx.com/latest\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getRates() {\n  const res = await fetch(\"https://api.fx.com/latest\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetRates: rates API) {\n    Fetch the <response> from the <url: \"https://api.fx.com/latest\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getRates() {\n  const res = await fetch(\"https://api.fx.com/latest\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetRates: rates API) {\n    Fetch the <response> from the <url: \"https://api.fx.com/latest\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getStock() {\n  const res = await fetch(\"https://api.stocks.io/quote\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetStock: stock API) {\n    Fetch the <response> from the <url: \"https://api.stocks.io/quote\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getStock() {\n  const res = await fetch(\"https://api.stocks.io/quote\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetStock: stock API) {\n    Fetch the <response> from the <url: \"https://api.stocks.io/quote\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getStock() {\n  const res = await fetch(\"https://api.stocks.io/quote\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetStock: stock API) {\n    Fetch the <response> from the <url: \"https://api.stocks.io/quote\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getNews() {\n  const res = await fetch(\"https://api.news.org/top\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetNews: news API) {\n    Fetch the <response> from the <url: \"https://api.news.org/top\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getNews() {\n  const res = await fetch(\"https://api.news.org/top\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetNews: news API) {\n    Fetch the <response> from the <url: \"https://api.news.org/top\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getNews() {\n  const res = await fetch(\"https://api.news.org/top\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetNews: news API) {\n    Fetch the <response> from the <url: \"https://api.news.org/top\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getGeo() {\n  const res = await fetch(\"https://api.geo.io/lookup\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetGeo: geo API) {\n    Fetch the <response> from the <url: \"https://api.geo.io/lookup\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getGeo() {\n  const res = await fetch(\"https://api.geo.io/lookup\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetGeo: geo API) {\n    Fetch the <response> from the <url: \"https://api.geo.io/lookup\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getGeo() {\n  const res = await fetch(\"https://api.geo.io/lookup\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetGeo: geo API) {\n    Fetch the <response> from the <url: \"https://api.geo.io/lookup\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getCrypto() {\n  const res = await fetch(\"https://api.coins.io/price\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetCrypto: crypto API) {\n    Fetch the <response> from the <url: \"https://api.coins.io/price\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getCrypto() {\n  const res = await fetch(\"https://api.coins.io/price\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetCrypto: crypto API) {\n    Fetch the <response> from the <url: \"https://api.coins.io/price\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getCrypto() {\n  const res = await fetch(\"https://api.coins.io/price\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetCrypto: crypto API) {\n    Fetch the <response> from the <url: \"https://api.coins.io/price\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getTraffic() {\n  const res = await fetch(\"https://api.maps.io/traffic\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetTraffic: traffic API) {\n    Fetch the <response> from the <url: \"https://api.maps.io/traffic\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getTraffic() {\n  const res = await fetch(\"https://api.maps.io/traffic\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetTraffic: traffic API) {\n    Fetch the <response> from the <url: \"https://api.maps.io/traffic\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getTraffic() {\n  const res = await fetch(\"https://api.maps.io/traffic\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetTraffic: traffic API) {\n    Fetch the <response> from the <url: \"https://api.maps.io/traffic\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nasync function getSports() {\n  const res = await fetch(\"https://api.scores.io/live\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetSports: sports API) {\n    Fetch the <response> from the <url: \"https://api.scores.io/live\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nasync function getSports() {\n  const res = await fetch(\"https://api.scores.io/live\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetSports: sports API) {\n    Fetch the <response> from the <url: \"https://api.scores.io/live\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nasync function getSports() {\n  const res = await fetch(\"https://api.scores.io/live\");\n  const json = await res.json();\n  return transform(json);\n}\n```", "output": "<think>\n`fetch(url)` is an external request \u2192 ARO REQUEST verb `Fetch ... from the <url>`. `res.json()` reads the body \u2192 `Extract the <payload> from the <response: body>`. `transform(json)` is an internal OWN transform \u2192 `Transform the <result>`. The JS return becomes `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(GetSports: sports API) {\n    Fetch the <response> from the <url: \"https://api.scores.io/live\">.\n    Extract the <payload> from the <response: body>.\n    Transform the <result> from the <payload>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "fetch", "return", "transform"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_status(users):\n    return [x for x in users if x[\"status\"] == \"active\"]\n```", "output": "<think>\nThe list comprehension keeps items where `status == \"active\"` \u2192 ARO `Filter ... where <status> = \"active\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterUsers: users API) {\n    Retrieve the <items> from the <users-repository>.\n    Filter the <filtered> from the <items> where <status> = \"active\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_status(users):\n    return [x for x in users if x[\"status\"] == \"active\"]\n```", "output": "<think>\nThe list comprehension keeps items where `status == \"active\"` \u2192 ARO `Filter ... where <status> = \"active\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterUsers: users API) {\n    Retrieve the <items> from the <users-repository>.\n    Filter the <filtered> from the <items> where <status> = \"active\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_status(users):\n    return [x for x in users if x[\"status\"] == \"active\"]\n```", "output": "<think>\nThe list comprehension keeps items where `status == \"active\"` \u2192 ARO `Filter ... where <status> = \"active\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterUsers: users API) {\n    Retrieve the <items> from the <users-repository>.\n    Filter the <filtered> from the <items> where <status> = \"active\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_state(orders):\n    return [x for x in orders if x[\"state\"] == \"paid\"]\n```", "output": "<think>\nThe list comprehension keeps items where `state == \"paid\"` \u2192 ARO `Filter ... where <state> = \"paid\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterOrders: orders API) {\n    Retrieve the <items> from the <orders-repository>.\n    Filter the <filtered> from the <items> where <state> = \"paid\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_state(orders):\n    return [x for x in orders if x[\"state\"] == \"paid\"]\n```", "output": "<think>\nThe list comprehension keeps items where `state == \"paid\"` \u2192 ARO `Filter ... where <state> = \"paid\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterOrders: orders API) {\n    Retrieve the <items> from the <orders-repository>.\n    Filter the <filtered> from the <items> where <state> = \"paid\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_state(orders):\n    return [x for x in orders if x[\"state\"] == \"paid\"]\n```", "output": "<think>\nThe list comprehension keeps items where `state == \"paid\"` \u2192 ARO `Filter ... where <state> = \"paid\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterOrders: orders API) {\n    Retrieve the <items> from the <orders-repository>.\n    Filter the <filtered> from the <items> where <state> = \"paid\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_category(products):\n    return [x for x in products if x[\"category\"] == \"books\"]\n```", "output": "<think>\nThe list comprehension keeps items where `category == \"books\"` \u2192 ARO `Filter ... where <category> = \"books\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterProducts: products API) {\n    Retrieve the <items> from the <products-repository>.\n    Filter the <filtered> from the <items> where <category> = \"books\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_category(products):\n    return [x for x in products if x[\"category\"] == \"books\"]\n```", "output": "<think>\nThe list comprehension keeps items where `category == \"books\"` \u2192 ARO `Filter ... where <category> = \"books\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterProducts: products API) {\n    Retrieve the <items> from the <products-repository>.\n    Filter the <filtered> from the <items> where <category> = \"books\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_category(products):\n    return [x for x in products if x[\"category\"] == \"books\"]\n```", "output": "<think>\nThe list comprehension keeps items where `category == \"books\"` \u2192 ARO `Filter ... where <category> = \"books\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterProducts: products API) {\n    Retrieve the <items> from the <products-repository>.\n    Filter the <filtered> from the <items> where <category> = \"books\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_level(events):\n    return [x for x in events if x[\"level\"] == \"critical\"]\n```", "output": "<think>\nThe list comprehension keeps items where `level == \"critical\"` \u2192 ARO `Filter ... where <level> = \"critical\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterEvents: events API) {\n    Retrieve the <items> from the <events-repository>.\n    Filter the <filtered> from the <items> where <level> = \"critical\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_level(events):\n    return [x for x in events if x[\"level\"] == \"critical\"]\n```", "output": "<think>\nThe list comprehension keeps items where `level == \"critical\"` \u2192 ARO `Filter ... where <level> = \"critical\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterEvents: events API) {\n    Retrieve the <items> from the <events-repository>.\n    Filter the <filtered> from the <items> where <level> = \"critical\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_level(events):\n    return [x for x in events if x[\"level\"] == \"critical\"]\n```", "output": "<think>\nThe list comprehension keeps items where `level == \"critical\"` \u2192 ARO `Filter ... where <level> = \"critical\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterEvents: events API) {\n    Retrieve the <items> from the <events-repository>.\n    Filter the <filtered> from the <items> where <level> = \"critical\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_priority(tickets):\n    return [x for x in tickets if x[\"priority\"] == \"high\"]\n```", "output": "<think>\nThe list comprehension keeps items where `priority == \"high\"` \u2192 ARO `Filter ... where <priority> = \"high\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterTickets: tickets API) {\n    Retrieve the <items> from the <tickets-repository>.\n    Filter the <filtered> from the <items> where <priority> = \"high\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_priority(tickets):\n    return [x for x in tickets if x[\"priority\"] == \"high\"]\n```", "output": "<think>\nThe list comprehension keeps items where `priority == \"high\"` \u2192 ARO `Filter ... where <priority> = \"high\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterTickets: tickets API) {\n    Retrieve the <items> from the <tickets-repository>.\n    Filter the <filtered> from the <items> where <priority> = \"high\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_priority(tickets):\n    return [x for x in tickets if x[\"priority\"] == \"high\"]\n```", "output": "<think>\nThe list comprehension keeps items where `priority == \"high\"` \u2192 ARO `Filter ... where <priority> = \"high\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterTickets: tickets API) {\n    Retrieve the <items> from the <tickets-repository>.\n    Filter the <filtered> from the <items> where <priority> = \"high\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_tier(accounts):\n    return [x for x in accounts if x[\"tier\"] == \"premium\"]\n```", "output": "<think>\nThe list comprehension keeps items where `tier == \"premium\"` \u2192 ARO `Filter ... where <tier> = \"premium\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterAccounts: accounts API) {\n    Retrieve the <items> from the <accounts-repository>.\n    Filter the <filtered> from the <items> where <tier> = \"premium\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_tier(accounts):\n    return [x for x in accounts if x[\"tier\"] == \"premium\"]\n```", "output": "<think>\nThe list comprehension keeps items where `tier == \"premium\"` \u2192 ARO `Filter ... where <tier> = \"premium\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterAccounts: accounts API) {\n    Retrieve the <items> from the <accounts-repository>.\n    Filter the <filtered> from the <items> where <tier> = \"premium\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_tier(accounts):\n    return [x for x in accounts if x[\"tier\"] == \"premium\"]\n```", "output": "<think>\nThe list comprehension keeps items where `tier == \"premium\"` \u2192 ARO `Filter ... where <tier> = \"premium\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterAccounts: accounts API) {\n    Retrieve the <items> from the <accounts-repository>.\n    Filter the <filtered> from the <items> where <tier> = \"premium\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_status(jobs):\n    return [x for x in jobs if x[\"status\"] == \"queued\"]\n```", "output": "<think>\nThe list comprehension keeps items where `status == \"queued\"` \u2192 ARO `Filter ... where <status> = \"queued\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterJobs: jobs API) {\n    Retrieve the <items> from the <jobs-repository>.\n    Filter the <filtered> from the <items> where <status> = \"queued\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_status(jobs):\n    return [x for x in jobs if x[\"status\"] == \"queued\"]\n```", "output": "<think>\nThe list comprehension keeps items where `status == \"queued\"` \u2192 ARO `Filter ... where <status> = \"queued\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterJobs: jobs API) {\n    Retrieve the <items> from the <jobs-repository>.\n    Filter the <filtered> from the <items> where <status> = \"queued\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_status(jobs):\n    return [x for x in jobs if x[\"status\"] == \"queued\"]\n```", "output": "<think>\nThe list comprehension keeps items where `status == \"queued\"` \u2192 ARO `Filter ... where <status> = \"queued\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterJobs: jobs API) {\n    Retrieve the <items> from the <jobs-repository>.\n    Filter the <filtered> from the <items> where <status> = \"queued\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef by_visibility(posts):\n    return [x for x in posts if x[\"visibility\"] == \"public\"]\n```", "output": "<think>\nThe list comprehension keeps items where `visibility == \"public\"` \u2192 ARO `Filter ... where <visibility> = \"public\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterPosts: posts API) {\n    Retrieve the <items> from the <posts-repository>.\n    Filter the <filtered> from the <items> where <visibility> = \"public\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef by_visibility(posts):\n    return [x for x in posts if x[\"visibility\"] == \"public\"]\n```", "output": "<think>\nThe list comprehension keeps items where `visibility == \"public\"` \u2192 ARO `Filter ... where <visibility> = \"public\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterPosts: posts API) {\n    Retrieve the <items> from the <posts-repository>.\n    Filter the <filtered> from the <items> where <visibility> = \"public\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef by_visibility(posts):\n    return [x for x in posts if x[\"visibility\"] == \"public\"]\n```", "output": "<think>\nThe list comprehension keeps items where `visibility == \"public\"` \u2192 ARO `Filter ... where <visibility> = \"public\"`. The items come from the repository \u2192 `Retrieve the <items>`. Returning the filtered list \u2192 `Return an <OK: status> with <filtered>`.\n</think>\n\n```aro\n(FilterPosts: posts API) {\n    Retrieve the <items> from the <posts-repository>.\n    Filter the <filtered> from the <items> where <visibility> = \"public\".\n    Return an <OK: status> with <filtered>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["filter", "retrieve", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createUser(req) {\n  const data = req.body;\n  const user = save(data);\n  emit(\"UserCreated\", user);\n  return { status: 201, body: user };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <user>` + `Store the <user> in the <user-repository>`. `emit(...)` \u2192 `Emit a <UserCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createUser: user API) {\n    Extract the <data> from the <request: body>.\n    Create the <user> with <data>.\n    Store the <user> into the <user-repository>.\n    Emit a <UserCreated: event> with <user>.\n    Return a <Created: status> with <user>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createUser(req) {\n  const data = req.body;\n  const user = save(data);\n  emit(\"UserCreated\", user);\n  return { status: 201, body: user };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <user>` + `Store the <user> in the <user-repository>`. `emit(...)` \u2192 `Emit a <UserCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createUser: user API) {\n    Extract the <data> from the <request: body>.\n    Create the <user> with <data>.\n    Store the <user> into the <user-repository>.\n    Emit a <UserCreated: event> with <user>.\n    Return a <Created: status> with <user>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createUser(req) {\n  const data = req.body;\n  const user = save(data);\n  emit(\"UserCreated\", user);\n  return { status: 201, body: user };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <user>` + `Store the <user> in the <user-repository>`. `emit(...)` \u2192 `Emit a <UserCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createUser: user API) {\n    Extract the <data> from the <request: body>.\n    Create the <user> with <data>.\n    Store the <user> into the <user-repository>.\n    Emit a <UserCreated: event> with <user>.\n    Return a <Created: status> with <user>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createOrder(req) {\n  const data = req.body;\n  const order = save(data);\n  emit(\"OrderCreated\", order);\n  return { status: 201, body: order };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <order>` + `Store the <order> in the <order-repository>`. `emit(...)` \u2192 `Emit a <OrderCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createOrder: order API) {\n    Extract the <data> from the <request: body>.\n    Create the <order> with <data>.\n    Store the <order> into the <order-repository>.\n    Emit a <OrderCreated: event> with <order>.\n    Return a <Created: status> with <order>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createOrder(req) {\n  const data = req.body;\n  const order = save(data);\n  emit(\"OrderCreated\", order);\n  return { status: 201, body: order };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <order>` + `Store the <order> in the <order-repository>`. `emit(...)` \u2192 `Emit a <OrderCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createOrder: order API) {\n    Extract the <data> from the <request: body>.\n    Create the <order> with <data>.\n    Store the <order> into the <order-repository>.\n    Emit a <OrderCreated: event> with <order>.\n    Return a <Created: status> with <order>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createOrder(req) {\n  const data = req.body;\n  const order = save(data);\n  emit(\"OrderCreated\", order);\n  return { status: 201, body: order };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <order>` + `Store the <order> in the <order-repository>`. `emit(...)` \u2192 `Emit a <OrderCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createOrder: order API) {\n    Extract the <data> from the <request: body>.\n    Create the <order> with <data>.\n    Store the <order> into the <order-repository>.\n    Emit a <OrderCreated: event> with <order>.\n    Return a <Created: status> with <order>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createPost(req) {\n  const data = req.body;\n  const post = save(data);\n  emit(\"PostCreated\", post);\n  return { status: 201, body: post };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <post>` + `Store the <post> in the <post-repository>`. `emit(...)` \u2192 `Emit a <PostCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createPost: post API) {\n    Extract the <data> from the <request: body>.\n    Create the <post> with <data>.\n    Store the <post> into the <post-repository>.\n    Emit a <PostCreated: event> with <post>.\n    Return a <Created: status> with <post>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createPost(req) {\n  const data = req.body;\n  const post = save(data);\n  emit(\"PostCreated\", post);\n  return { status: 201, body: post };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <post>` + `Store the <post> in the <post-repository>`. `emit(...)` \u2192 `Emit a <PostCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createPost: post API) {\n    Extract the <data> from the <request: body>.\n    Create the <post> with <data>.\n    Store the <post> into the <post-repository>.\n    Emit a <PostCreated: event> with <post>.\n    Return a <Created: status> with <post>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createPost(req) {\n  const data = req.body;\n  const post = save(data);\n  emit(\"PostCreated\", post);\n  return { status: 201, body: post };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <post>` + `Store the <post> in the <post-repository>`. `emit(...)` \u2192 `Emit a <PostCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createPost: post API) {\n    Extract the <data> from the <request: body>.\n    Create the <post> with <data>.\n    Store the <post> into the <post-repository>.\n    Emit a <PostCreated: event> with <post>.\n    Return a <Created: status> with <post>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createTicket(req) {\n  const data = req.body;\n  const ticket = save(data);\n  emit(\"TicketCreated\", ticket);\n  return { status: 201, body: ticket };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <ticket>` + `Store the <ticket> in the <ticket-repository>`. `emit(...)` \u2192 `Emit a <TicketCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createTicket: ticket API) {\n    Extract the <data> from the <request: body>.\n    Create the <ticket> with <data>.\n    Store the <ticket> into the <ticket-repository>.\n    Emit a <TicketCreated: event> with <ticket>.\n    Return a <Created: status> with <ticket>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createTicket(req) {\n  const data = req.body;\n  const ticket = save(data);\n  emit(\"TicketCreated\", ticket);\n  return { status: 201, body: ticket };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <ticket>` + `Store the <ticket> in the <ticket-repository>`. `emit(...)` \u2192 `Emit a <TicketCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createTicket: ticket API) {\n    Extract the <data> from the <request: body>.\n    Create the <ticket> with <data>.\n    Store the <ticket> into the <ticket-repository>.\n    Emit a <TicketCreated: event> with <ticket>.\n    Return a <Created: status> with <ticket>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createTicket(req) {\n  const data = req.body;\n  const ticket = save(data);\n  emit(\"TicketCreated\", ticket);\n  return { status: 201, body: ticket };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <ticket>` + `Store the <ticket> in the <ticket-repository>`. `emit(...)` \u2192 `Emit a <TicketCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createTicket: ticket API) {\n    Extract the <data> from the <request: body>.\n    Create the <ticket> with <data>.\n    Store the <ticket> into the <ticket-repository>.\n    Emit a <TicketCreated: event> with <ticket>.\n    Return a <Created: status> with <ticket>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createComment(req) {\n  const data = req.body;\n  const comment = save(data);\n  emit(\"CommentCreated\", comment);\n  return { status: 201, body: comment };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <comment>` + `Store the <comment> in the <comment-repository>`. `emit(...)` \u2192 `Emit a <CommentCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createComment: comment API) {\n    Extract the <data> from the <request: body>.\n    Create the <comment> with <data>.\n    Store the <comment> into the <comment-repository>.\n    Emit a <CommentCreated: event> with <comment>.\n    Return a <Created: status> with <comment>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createComment(req) {\n  const data = req.body;\n  const comment = save(data);\n  emit(\"CommentCreated\", comment);\n  return { status: 201, body: comment };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <comment>` + `Store the <comment> in the <comment-repository>`. `emit(...)` \u2192 `Emit a <CommentCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createComment: comment API) {\n    Extract the <data> from the <request: body>.\n    Create the <comment> with <data>.\n    Store the <comment> into the <comment-repository>.\n    Emit a <CommentCreated: event> with <comment>.\n    Return a <Created: status> with <comment>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createComment(req) {\n  const data = req.body;\n  const comment = save(data);\n  emit(\"CommentCreated\", comment);\n  return { status: 201, body: comment };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <comment>` + `Store the <comment> in the <comment-repository>`. `emit(...)` \u2192 `Emit a <CommentCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createComment: comment API) {\n    Extract the <data> from the <request: body>.\n    Create the <comment> with <data>.\n    Store the <comment> into the <comment-repository>.\n    Emit a <CommentCreated: event> with <comment>.\n    Return a <Created: status> with <comment>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createInvoice(req) {\n  const data = req.body;\n  const invoice = save(data);\n  emit(\"InvoiceCreated\", invoice);\n  return { status: 201, body: invoice };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <invoice>` + `Store the <invoice> in the <invoice-repository>`. `emit(...)` \u2192 `Emit a <InvoiceCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createInvoice: invoice API) {\n    Extract the <data> from the <request: body>.\n    Create the <invoice> with <data>.\n    Store the <invoice> into the <invoice-repository>.\n    Emit a <InvoiceCreated: event> with <invoice>.\n    Return a <Created: status> with <invoice>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createInvoice(req) {\n  const data = req.body;\n  const invoice = save(data);\n  emit(\"InvoiceCreated\", invoice);\n  return { status: 201, body: invoice };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <invoice>` + `Store the <invoice> in the <invoice-repository>`. `emit(...)` \u2192 `Emit a <InvoiceCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createInvoice: invoice API) {\n    Extract the <data> from the <request: body>.\n    Create the <invoice> with <data>.\n    Store the <invoice> into the <invoice-repository>.\n    Emit a <InvoiceCreated: event> with <invoice>.\n    Return a <Created: status> with <invoice>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createInvoice(req) {\n  const data = req.body;\n  const invoice = save(data);\n  emit(\"InvoiceCreated\", invoice);\n  return { status: 201, body: invoice };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <invoice>` + `Store the <invoice> in the <invoice-repository>`. `emit(...)` \u2192 `Emit a <InvoiceCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createInvoice: invoice API) {\n    Extract the <data> from the <request: body>.\n    Create the <invoice> with <data>.\n    Store the <invoice> into the <invoice-repository>.\n    Emit a <InvoiceCreated: event> with <invoice>.\n    Return a <Created: status> with <invoice>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createBooking(req) {\n  const data = req.body;\n  const booking = save(data);\n  emit(\"BookingCreated\", booking);\n  return { status: 201, body: booking };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <booking>` + `Store the <booking> in the <booking-repository>`. `emit(...)` \u2192 `Emit a <BookingCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createBooking: booking API) {\n    Extract the <data> from the <request: body>.\n    Create the <booking> with <data>.\n    Store the <booking> into the <booking-repository>.\n    Emit a <BookingCreated: event> with <booking>.\n    Return a <Created: status> with <booking>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createBooking(req) {\n  const data = req.body;\n  const booking = save(data);\n  emit(\"BookingCreated\", booking);\n  return { status: 201, body: booking };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <booking>` + `Store the <booking> in the <booking-repository>`. `emit(...)` \u2192 `Emit a <BookingCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createBooking: booking API) {\n    Extract the <data> from the <request: body>.\n    Create the <booking> with <data>.\n    Store the <booking> into the <booking-repository>.\n    Emit a <BookingCreated: event> with <booking>.\n    Return a <Created: status> with <booking>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createBooking(req) {\n  const data = req.body;\n  const booking = save(data);\n  emit(\"BookingCreated\", booking);\n  return { status: 201, body: booking };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <booking>` + `Store the <booking> in the <booking-repository>`. `emit(...)` \u2192 `Emit a <BookingCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createBooking: booking API) {\n    Extract the <data> from the <request: body>.\n    Create the <booking> with <data>.\n    Store the <booking> into the <booking-repository>.\n    Emit a <BookingCreated: event> with <booking>.\n    Return a <Created: status> with <booking>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction createReview(req) {\n  const data = req.body;\n  const review = save(data);\n  emit(\"ReviewCreated\", review);\n  return { status: 201, body: review };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <review>` + `Store the <review> in the <review-repository>`. `emit(...)` \u2192 `Emit a <ReviewCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createReview: review API) {\n    Extract the <data> from the <request: body>.\n    Create the <review> with <data>.\n    Store the <review> into the <review-repository>.\n    Emit a <ReviewCreated: event> with <review>.\n    Return a <Created: status> with <review>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction createReview(req) {\n  const data = req.body;\n  const review = save(data);\n  emit(\"ReviewCreated\", review);\n  return { status: 201, body: review };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <review>` + `Store the <review> in the <review-repository>`. `emit(...)` \u2192 `Emit a <ReviewCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createReview: review API) {\n    Extract the <data> from the <request: body>.\n    Create the <review> with <data>.\n    Store the <review> into the <review-repository>.\n    Emit a <ReviewCreated: event> with <review>.\n    Return a <Created: status> with <review>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction createReview(req) {\n  const data = req.body;\n  const review = save(data);\n  emit(\"ReviewCreated\", review);\n  return { status: 201, body: review };\n}\n```", "output": "<think>\n`req.body` \u2192 `Extract the <data> from the <request: body>`. `save(data)` both creates and persists \u2192 `Create the <review>` + `Store the <review> in the <review-repository>`. `emit(...)` \u2192 `Emit a <ReviewCreated: event>`. HTTP 201 \u2192 `Return a <Created: status>`.\n</think>\n\n```aro\n(createReview: review API) {\n    Extract the <data> from the <request: body>.\n    Create the <review> with <data>.\n    Store the <review> into the <review-repository>.\n    Emit a <ReviewCreated: event> with <review>.\n    Return a <Created: status> with <review>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["create", "emit", "extract", "return", "store"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef register(user):\n    pw = hashlib.sha256(user[\"password\"].encode()).hexdigest()\n    user[\"password\"] = pw\n    db.save(user)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-user> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <user-repository>`.\n</think>\n\n```aro\n(registerUser: user API) {\n    Extract the <user> from the <request: body>.\n    Extract the <password> from the <user: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-user> with { password: <digest> }.\n    Store the <secured-user> into the <user-repository>.\n    Return an <OK: status> with <secured-user>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef register(user):\n    pw = hashlib.sha256(user[\"password\"].encode()).hexdigest()\n    user[\"password\"] = pw\n    db.save(user)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-user> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <user-repository>`.\n</think>\n\n```aro\n(registerUser: user API) {\n    Extract the <user> from the <request: body>.\n    Extract the <password> from the <user: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-user> with { password: <digest> }.\n    Store the <secured-user> into the <user-repository>.\n    Return an <OK: status> with <secured-user>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef register(user):\n    pw = hashlib.sha256(user[\"password\"].encode()).hexdigest()\n    user[\"password\"] = pw\n    db.save(user)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-user> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <user-repository>`.\n</think>\n\n```aro\n(registerUser: user API) {\n    Extract the <user> from the <request: body>.\n    Extract the <password> from the <user: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-user> with { password: <digest> }.\n    Store the <secured-user> into the <user-repository>.\n    Return an <OK: status> with <secured-user>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef register(account):\n    pw = hashlib.sha256(account[\"password\"].encode()).hexdigest()\n    account[\"password\"] = pw\n    db.save(account)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-account> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <account-repository>`.\n</think>\n\n```aro\n(registerAccount: account API) {\n    Extract the <account> from the <request: body>.\n    Extract the <password> from the <account: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-account> with { password: <digest> }.\n    Store the <secured-account> into the <account-repository>.\n    Return an <OK: status> with <secured-account>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef register(account):\n    pw = hashlib.sha256(account[\"password\"].encode()).hexdigest()\n    account[\"password\"] = pw\n    db.save(account)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-account> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <account-repository>`.\n</think>\n\n```aro\n(registerAccount: account API) {\n    Extract the <account> from the <request: body>.\n    Extract the <password> from the <account: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-account> with { password: <digest> }.\n    Store the <secured-account> into the <account-repository>.\n    Return an <OK: status> with <secured-account>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef register(account):\n    pw = hashlib.sha256(account[\"password\"].encode()).hexdigest()\n    account[\"password\"] = pw\n    db.save(account)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-account> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <account-repository>`.\n</think>\n\n```aro\n(registerAccount: account API) {\n    Extract the <account> from the <request: body>.\n    Extract the <password> from the <account: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-account> with { password: <digest> }.\n    Store the <secured-account> into the <account-repository>.\n    Return an <OK: status> with <secured-account>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef register(member):\n    pw = hashlib.sha256(member[\"password\"].encode()).hexdigest()\n    member[\"password\"] = pw\n    db.save(member)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-member> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <member-repository>`.\n</think>\n\n```aro\n(registerMember: member API) {\n    Extract the <member> from the <request: body>.\n    Extract the <password> from the <member: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-member> with { password: <digest> }.\n    Store the <secured-member> into the <member-repository>.\n    Return an <OK: status> with <secured-member>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef register(member):\n    pw = hashlib.sha256(member[\"password\"].encode()).hexdigest()\n    member[\"password\"] = pw\n    db.save(member)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-member> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <member-repository>`.\n</think>\n\n```aro\n(registerMember: member API) {\n    Extract the <member> from the <request: body>.\n    Extract the <password> from the <member: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-member> with { password: <digest> }.\n    Store the <secured-member> into the <member-repository>.\n    Return an <OK: status> with <secured-member>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef register(member):\n    pw = hashlib.sha256(member[\"password\"].encode()).hexdigest()\n    member[\"password\"] = pw\n    db.save(member)\n```", "output": "<think>\n`hashlib.sha256(...).hexdigest()` hashes the password \u2192 ARO `Compute the <digest: hash> from <password>`. Reassigning the field is immutable in ARO, so build a new record with `Create the <secured-member> with { password: <digest> }`. `db.save` \u2192 `Store ... into the <member-repository>`.\n</think>\n\n```aro\n(registerMember: member API) {\n    Extract the <member> from the <request: body>.\n    Extract the <password> from the <member: password>.\n    Compute the <digest: hash> from <password>.\n    Create the <secured-member> with { password: <digest> }.\n    Store the <secured-member> into the <member-repository>.\n    Return an <OK: status> with <secured-member>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "create", "extract", "return", "store"]}
{"instruction": "Translate this Go to ARO:\n```go\nfunc process(orders []Item) {\n    for _, item := range orders {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <orders>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <orders>`.\n</think>\n\n```aro\n(ProcessOrders: orders API) {\n    Retrieve the <orders> from the <orders-repository>.\n    For each <item> in <orders> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <orders>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Rewrite this Go function as an ARO feature set:\n```go\nfunc process(orders []Item) {\n    for _, item := range orders {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <orders>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <orders>`.\n</think>\n\n```aro\n(ProcessOrders: orders API) {\n    Retrieve the <orders> from the <orders-repository>.\n    For each <item> in <orders> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <orders>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Port this Go to ARO, preserving its behaviour:\n```go\nfunc process(orders []Item) {\n    for _, item := range orders {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <orders>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <orders>`.\n</think>\n\n```aro\n(ProcessOrders: orders API) {\n    Retrieve the <orders> from the <orders-repository>.\n    For each <item> in <orders> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <orders>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Translate this Go to ARO:\n```go\nfunc process(accounts []Item) {\n    for _, item := range accounts {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <accounts>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <accounts>`.\n</think>\n\n```aro\n(ProcessUsers: users API) {\n    Retrieve the <accounts> from the <users-repository>.\n    For each <item> in <accounts> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <users>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Rewrite this Go function as an ARO feature set:\n```go\nfunc process(accounts []Item) {\n    for _, item := range accounts {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <accounts>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <accounts>`.\n</think>\n\n```aro\n(ProcessUsers: users API) {\n    Retrieve the <accounts> from the <users-repository>.\n    For each <item> in <accounts> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <users>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Port this Go to ARO, preserving its behaviour:\n```go\nfunc process(accounts []Item) {\n    for _, item := range accounts {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <accounts>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <accounts>`.\n</think>\n\n```aro\n(ProcessUsers: users API) {\n    Retrieve the <accounts> from the <users-repository>.\n    For each <item> in <accounts> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <users>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Translate this Go to ARO:\n```go\nfunc process(queue []Item) {\n    for _, item := range queue {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <queue>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <queue>`.\n</think>\n\n```aro\n(ProcessJobs: jobs API) {\n    Retrieve the <queue> from the <jobs-repository>.\n    For each <item> in <queue> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <jobs>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Rewrite this Go function as an ARO feature set:\n```go\nfunc process(queue []Item) {\n    for _, item := range queue {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <queue>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <queue>`.\n</think>\n\n```aro\n(ProcessJobs: jobs API) {\n    Retrieve the <queue> from the <jobs-repository>.\n    For each <item> in <queue> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <jobs>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Port this Go to ARO, preserving its behaviour:\n```go\nfunc process(queue []Item) {\n    for _, item := range queue {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <queue>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <queue>`.\n</think>\n\n```aro\n(ProcessJobs: jobs API) {\n    Retrieve the <queue> from the <jobs-repository>.\n    For each <item> in <queue> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <jobs>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Translate this Go to ARO:\n```go\nfunc process(alerts []Item) {\n    for _, item := range alerts {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <alerts>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <alerts>`.\n</think>\n\n```aro\n(ProcessAlerts: alerts API) {\n    Retrieve the <alerts> from the <alerts-repository>.\n    For each <item> in <alerts> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <alerts>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Rewrite this Go function as an ARO feature set:\n```go\nfunc process(alerts []Item) {\n    for _, item := range alerts {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <alerts>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <alerts>`.\n</think>\n\n```aro\n(ProcessAlerts: alerts API) {\n    Retrieve the <alerts> from the <alerts-repository>.\n    For each <item> in <alerts> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <alerts>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Port this Go to ARO, preserving its behaviour:\n```go\nfunc process(alerts []Item) {\n    for _, item := range alerts {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <alerts>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <alerts>`.\n</think>\n\n```aro\n(ProcessAlerts: alerts API) {\n    Retrieve the <alerts> from the <alerts-repository>.\n    For each <item> in <alerts> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <alerts>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Translate this Go to ARO:\n```go\nfunc process(events []Item) {\n    for _, item := range events {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <events>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <events>`.\n</think>\n\n```aro\n(ProcessEvents: events API) {\n    Retrieve the <events> from the <events-repository>.\n    For each <item> in <events> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <events>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Rewrite this Go function as an ARO feature set:\n```go\nfunc process(events []Item) {\n    for _, item := range events {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <events>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <events>`.\n</think>\n\n```aro\n(ProcessEvents: events API) {\n    Retrieve the <events> from the <events-repository>.\n    For each <item> in <events> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <events>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Port this Go to ARO, preserving its behaviour:\n```go\nfunc process(events []Item) {\n    for _, item := range events {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <events>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <events>`.\n</think>\n\n```aro\n(ProcessEvents: events API) {\n    Retrieve the <events> from the <events-repository>.\n    For each <item> in <events> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <events>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Translate this Go to ARO:\n```go\nfunc process(transactions []Item) {\n    for _, item := range transactions {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <transactions>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <transactions>`.\n</think>\n\n```aro\n(ProcessPayments: payments API) {\n    Retrieve the <transactions> from the <payments-repository>.\n    For each <item> in <transactions> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <payments>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Rewrite this Go function as an ARO feature set:\n```go\nfunc process(transactions []Item) {\n    for _, item := range transactions {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <transactions>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <transactions>`.\n</think>\n\n```aro\n(ProcessPayments: payments API) {\n    Retrieve the <transactions> from the <payments-repository>.\n    For each <item> in <transactions> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <payments>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Port this Go to ARO, preserving its behaviour:\n```go\nfunc process(transactions []Item) {\n    for _, item := range transactions {\n        log.Println(item)\n    }\n}\n```", "output": "<think>\nThe Go `for range` loop iterates the slice \u2192 ARO `For each <item> in <transactions>`. `log.Println(item)` \u2192 `Log <item> to the <console>`. The slice comes from the repository \u2192 `Retrieve the <transactions>`.\n</think>\n\n```aro\n(ProcessPayments: payments API) {\n    Retrieve the <transactions> from the <payments-repository>.\n    For each <item> in <transactions> {\n        Log <item> to the <console>.\n    }\n    Return an <OK: status> for the <payments>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["log", "retrieve", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction add(a, b) {\n  return a + b;\n}\n```", "output": "<think>\nThe function returns `a + b` \u2192 ARO arithmetic `Compute the <result> from <a> + <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Add: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> + <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction add(a, b) {\n  return a + b;\n}\n```", "output": "<think>\nThe function returns `a + b` \u2192 ARO arithmetic `Compute the <result> from <a> + <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Add: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> + <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction add(a, b) {\n  return a + b;\n}\n```", "output": "<think>\nThe function returns `a + b` \u2192 ARO arithmetic `Compute the <result> from <a> + <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Add: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> + <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction subtract(a, b) {\n  return a - b;\n}\n```", "output": "<think>\nThe function returns `a - b` \u2192 ARO arithmetic `Compute the <result> from <a> - <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Subtract: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> - <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction subtract(a, b) {\n  return a - b;\n}\n```", "output": "<think>\nThe function returns `a - b` \u2192 ARO arithmetic `Compute the <result> from <a> - <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Subtract: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> - <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction subtract(a, b) {\n  return a - b;\n}\n```", "output": "<think>\nThe function returns `a - b` \u2192 ARO arithmetic `Compute the <result> from <a> - <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Subtract: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> - <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction multiply(a, b) {\n  return a * b;\n}\n```", "output": "<think>\nThe function returns `a * b` \u2192 ARO arithmetic `Compute the <result> from <a> * <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Multiply: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> * <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction multiply(a, b) {\n  return a * b;\n}\n```", "output": "<think>\nThe function returns `a * b` \u2192 ARO arithmetic `Compute the <result> from <a> * <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Multiply: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> * <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction multiply(a, b) {\n  return a * b;\n}\n```", "output": "<think>\nThe function returns `a * b` \u2192 ARO arithmetic `Compute the <result> from <a> * <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Multiply: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> * <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction divide(a, b) {\n  return a / b;\n}\n```", "output": "<think>\nThe function returns `a / b` \u2192 ARO arithmetic `Compute the <result> from <a> / <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Divide: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> / <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction divide(a, b) {\n  return a / b;\n}\n```", "output": "<think>\nThe function returns `a / b` \u2192 ARO arithmetic `Compute the <result> from <a> / <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Divide: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> / <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction divide(a, b) {\n  return a / b;\n}\n```", "output": "<think>\nThe function returns `a / b` \u2192 ARO arithmetic `Compute the <result> from <a> / <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Divide: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> / <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Translate this JavaScript to ARO:\n```javascript\nfunction modulo(a, b) {\n  return a % b;\n}\n```", "output": "<think>\nThe function returns `a % b` \u2192 ARO arithmetic `Compute the <result> from <a> % <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Modulo: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> % <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Rewrite this JavaScript function as an ARO feature set:\n```javascript\nfunction modulo(a, b) {\n  return a % b;\n}\n```", "output": "<think>\nThe function returns `a % b` \u2192 ARO arithmetic `Compute the <result> from <a> % <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Modulo: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> % <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Port this JavaScript to ARO, preserving its behaviour:\n```javascript\nfunction modulo(a, b) {\n  return a % b;\n}\n```", "output": "<think>\nThe function returns `a % b` \u2192 ARO arithmetic `Compute the <result> from <a> % <b>`. The two operands are inputs \u2192 `Extract the <a>` / `Extract the <b>`. The return \u2192 `Return an <OK: status> with <result>`.\n</think>\n\n```aro\n(Modulo: math API) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <result> from <a> % <b>.\n    Return an <OK: status> with <result>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["compute", "extract", "return"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef submit(order):\n    if not order[\"email\"]:\n        raise ValueError(\"email required\")\n    return save(order)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <email> == \"\"` guard. `save(order)` \u2192 `Store the <order> into the <order-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitOrder: order API) {\n    Extract the <order> from the <request: body>.\n    Extract the <email> from the <order: email>.\n    Throw a <BadRequest: status> for the <order> when <email> == \"\".\n    Store the <order> into the <order-repository>.\n    Return an <OK: status> with <order>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef submit(order):\n    if not order[\"email\"]:\n        raise ValueError(\"email required\")\n    return save(order)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <email> == \"\"` guard. `save(order)` \u2192 `Store the <order> into the <order-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitOrder: order API) {\n    Extract the <order> from the <request: body>.\n    Extract the <email> from the <order: email>.\n    Throw a <BadRequest: status> for the <order> when <email> == \"\".\n    Store the <order> into the <order-repository>.\n    Return an <OK: status> with <order>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef submit(order):\n    if not order[\"email\"]:\n        raise ValueError(\"email required\")\n    return save(order)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <email> == \"\"` guard. `save(order)` \u2192 `Store the <order> into the <order-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitOrder: order API) {\n    Extract the <order> from the <request: body>.\n    Extract the <email> from the <order: email>.\n    Throw a <BadRequest: status> for the <order> when <email> == \"\".\n    Store the <order> into the <order-repository>.\n    Return an <OK: status> with <order>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef submit(account):\n    if not account[\"username\"]:\n        raise ValueError(\"username required\")\n    return save(account)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <username> == \"\"` guard. `save(account)` \u2192 `Store the <account> into the <account-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitAccount: account API) {\n    Extract the <account> from the <request: body>.\n    Extract the <username> from the <account: username>.\n    Throw a <BadRequest: status> for the <account> when <username> == \"\".\n    Store the <account> into the <account-repository>.\n    Return an <OK: status> with <account>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef submit(account):\n    if not account[\"username\"]:\n        raise ValueError(\"username required\")\n    return save(account)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <username> == \"\"` guard. `save(account)` \u2192 `Store the <account> into the <account-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitAccount: account API) {\n    Extract the <account> from the <request: body>.\n    Extract the <username> from the <account: username>.\n    Throw a <BadRequest: status> for the <account> when <username> == \"\".\n    Store the <account> into the <account-repository>.\n    Return an <OK: status> with <account>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef submit(account):\n    if not account[\"username\"]:\n        raise ValueError(\"username required\")\n    return save(account)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <username> == \"\"` guard. `save(account)` \u2192 `Store the <account> into the <account-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitAccount: account API) {\n    Extract the <account> from the <request: body>.\n    Extract the <username> from the <account: username>.\n    Throw a <BadRequest: status> for the <account> when <username> == \"\".\n    Store the <account> into the <account-repository>.\n    Return an <OK: status> with <account>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef submit(payment):\n    if not payment[\"amount\"]:\n        raise ValueError(\"amount required\")\n    return save(payment)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <amount> == \"\"` guard. `save(payment)` \u2192 `Store the <payment> into the <payment-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitPayment: payment API) {\n    Extract the <payment> from the <request: body>.\n    Extract the <amount> from the <payment: amount>.\n    Throw a <BadRequest: status> for the <payment> when <amount> == \"\".\n    Store the <payment> into the <payment-repository>.\n    Return an <OK: status> with <payment>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef submit(payment):\n    if not payment[\"amount\"]:\n        raise ValueError(\"amount required\")\n    return save(payment)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <amount> == \"\"` guard. `save(payment)` \u2192 `Store the <payment> into the <payment-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitPayment: payment API) {\n    Extract the <payment> from the <request: body>.\n    Extract the <amount> from the <payment: amount>.\n    Throw a <BadRequest: status> for the <payment> when <amount> == \"\".\n    Store the <payment> into the <payment-repository>.\n    Return an <OK: status> with <payment>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef submit(payment):\n    if not payment[\"amount\"]:\n        raise ValueError(\"amount required\")\n    return save(payment)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <amount> == \"\"` guard. `save(payment)` \u2192 `Store the <payment> into the <payment-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitPayment: payment API) {\n    Extract the <payment> from the <request: body>.\n    Extract the <amount> from the <payment: amount>.\n    Throw a <BadRequest: status> for the <payment> when <amount> == \"\".\n    Store the <payment> into the <payment-repository>.\n    Return an <OK: status> with <payment>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef submit(booking):\n    if not booking[\"date\"]:\n        raise ValueError(\"date required\")\n    return save(booking)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <date> == \"\"` guard. `save(booking)` \u2192 `Store the <booking> into the <booking-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitBooking: booking API) {\n    Extract the <booking> from the <request: body>.\n    Extract the <date> from the <booking: date>.\n    Throw a <BadRequest: status> for the <booking> when <date> == \"\".\n    Store the <booking> into the <booking-repository>.\n    Return an <OK: status> with <booking>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef submit(booking):\n    if not booking[\"date\"]:\n        raise ValueError(\"date required\")\n    return save(booking)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <date> == \"\"` guard. `save(booking)` \u2192 `Store the <booking> into the <booking-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitBooking: booking API) {\n    Extract the <booking> from the <request: body>.\n    Extract the <date> from the <booking: date>.\n    Throw a <BadRequest: status> for the <booking> when <date> == \"\".\n    Store the <booking> into the <booking-repository>.\n    Return an <OK: status> with <booking>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef submit(booking):\n    if not booking[\"date\"]:\n        raise ValueError(\"date required\")\n    return save(booking)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <date> == \"\"` guard. `save(booking)` \u2192 `Store the <booking> into the <booking-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitBooking: booking API) {\n    Extract the <booking> from the <request: body>.\n    Extract the <date> from the <booking: date>.\n    Throw a <BadRequest: status> for the <booking> when <date> == \"\".\n    Store the <booking> into the <booking-repository>.\n    Return an <OK: status> with <booking>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef submit(signup):\n    if not signup[\"name\"]:\n        raise ValueError(\"name required\")\n    return save(signup)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <name> == \"\"` guard. `save(signup)` \u2192 `Store the <signup> into the <signup-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitSignup: signup API) {\n    Extract the <signup> from the <request: body>.\n    Extract the <name> from the <signup: name>.\n    Throw a <BadRequest: status> for the <signup> when <name> == \"\".\n    Store the <signup> into the <signup-repository>.\n    Return an <OK: status> with <signup>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef submit(signup):\n    if not signup[\"name\"]:\n        raise ValueError(\"name required\")\n    return save(signup)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <name> == \"\"` guard. `save(signup)` \u2192 `Store the <signup> into the <signup-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitSignup: signup API) {\n    Extract the <signup> from the <request: body>.\n    Extract the <name> from the <signup: name>.\n    Throw a <BadRequest: status> for the <signup> when <name> == \"\".\n    Store the <signup> into the <signup-repository>.\n    Return an <OK: status> with <signup>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef submit(signup):\n    if not signup[\"name\"]:\n        raise ValueError(\"name required\")\n    return save(signup)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <name> == \"\"` guard. `save(signup)` \u2192 `Store the <signup> into the <signup-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitSignup: signup API) {\n    Extract the <signup> from the <request: body>.\n    Extract the <name> from the <signup: name>.\n    Throw a <BadRequest: status> for the <signup> when <name> == \"\".\n    Store the <signup> into the <signup-repository>.\n    Return an <OK: status> with <signup>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Translate this Python to ARO:\n```python\ndef submit(request):\n    if not request[\"token\"]:\n        raise ValueError(\"token required\")\n    return save(request)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <token> == \"\"` guard. `save(request)` \u2192 `Store the <request> into the <request-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitRequest: request API) {\n    Extract the <request> from the <request: body>.\n    Extract the <token> from the <request: token>.\n    Throw a <BadRequest: status> for the <request> when <token> == \"\".\n    Store the <request> into the <request-repository>.\n    Return an <OK: status> with <request>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Rewrite this Python function as an ARO feature set:\n```python\ndef submit(request):\n    if not request[\"token\"]:\n        raise ValueError(\"token required\")\n    return save(request)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <token> == \"\"` guard. `save(request)` \u2192 `Store the <request> into the <request-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitRequest: request API) {\n    Extract the <request> from the <request: body>.\n    Extract the <token> from the <request: token>.\n    Throw a <BadRequest: status> for the <request> when <token> == \"\".\n    Store the <request> into the <request-repository>.\n    Return an <OK: status> with <request>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
{"instruction": "Port this Python to ARO, preserving its behaviour:\n```python\ndef submit(request):\n    if not request[\"token\"]:\n        raise ValueError(\"token required\")\n    return save(request)\n```", "output": "<think>\nThe `if not ...: raise` guard maps to an ARO `Throw ... when <token> == \"\"` guard. `save(request)` \u2192 `Store the <request> into the <request-repository>`. The happy-case return \u2192 `Return an <OK: status>`.\n</think>\n\n```aro\n(submitRequest: request API) {\n    Extract the <request> from the <request: body>.\n    Extract the <token> from the <request: token>.\n    Throw a <BadRequest: status> for the <request> when <token> == \"\".\n    Store the <request> into the <request-repository>.\n    Return an <OK: status> with <request>.\n}\n```", "category": "translation", "task_type": "translation", "source": "eval_gen_translation", "intent_verbs": ["extract", "return", "store", "throw"]}
