{"timestamp":"2026-05-23T17:08:28Z","fixed_output":"```aro\n(Compute: Add Two Integers) {\n    Extract the <a> from the <request: body>.\n    Extract the <b> from the <request: body>.\n    Compute the <sum> with <a> +<b>.\n    Return an<OK: status> with the <sum>.\n}\n```","attempts":3,"broken_output":"```aro\n(Compute: Add Two Integers) {\n    Extract the <a> from the <request: body>.\n    Extract the <b> from the <request: body>.\n    Compute the <sum> with a + b.\n    Return an<OK: status> with the <sum>.\n}\n```","error_prompt":"The user's original request was:\n\nGive me an example of a Compute action that adds two integers.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  4:28: error: Expected preposition, but got +\n  2:5: warning: Variable 'a' is defined but never used\n  3:5: warning: Variable 'b' is defined but never used\n  Found 1 error(s), 2 warning(s) in main.aro\n\n❌ 1 error(s) found\n⚠️  2 warning(s) found\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block."}
{"fixed_output":"```aro\n(AsyncHandler: HandleEvent) {\n    Extract the <event> from the <event: event>.\n    async HandleEvent {\n        await HandleEventAsync();\n    }\n    Return an<OK: status> for the <result>.\n}\n```","attempts":3,"timestamp":"2026-05-23T17:24:37Z","broken_output":"```aro\n(AsyncEvent Handler: HandleEvent) {\n    Extract the <event> from the <event: event>.\n    async HandleEvent {\n        await HandleEventAsync();\n    }\n    Return an<OK: status> for the <result>.\n}\n```","error_prompt":"The user's original request was:\n\nDoes ARO support concurrency?\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  2:5: error: Expected action verb (e.g., Extract, Filter, Return), but got identifier(async)\n  5:1: error: Expected '(', but got }\n  4:6: warning: Feature set 'AsyncEvent Handler' has no Return or Throw statement\n    hint: Feature sets should end with a Return statement\n    hint: Add: <Return> an <OK: status> for the <result>.\n  Found 2 error(s), 1 warning(s) in main.aro\n\n❌ 2 error(s) found\n⚠️  1 warning(s) found\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block."}
{"attempts":3,"timestamp":"2026-05-23T17:25:35Z","error_prompt":"The user's original request was:\n\nInsert a Compute step that doubles a value between Extract and Return.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  3:32: error: Expected preposition, but got *\n  3:33: error: Expected action verb (e.g., Extract, Filter, Return), but got <\n  2:5: warning: Variable 'value' is defined but never used\n   warning: External dependency 'doubled' is not published by any feature set\n    hint: Consider adding a <Publish> statement or marking it as framework-provided\n  Found 2 error(s), 2 warning(s) in main.aro\n\n❌ 2 error(s) found\n⚠️  2 warning(s) found\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block.","fixed_output":"```aro\n(Return: Hello World) {\n    Extract the <value> from the <request: body>.\n    Compute the <doubled> as 2 * the <value>.\n    Return the<OK: status> with <doubled>.\n}\n```","broken_output":"```aro\n(Return: Hello World) {\n    Extract the <value> from the <request: body>.\n    Compute the <doubled> as 2 *<value>.\n    Return the<OK: status> with <doubled>.\n}\n```"}
{"timestamp":"2026-05-23T17:28:14Z","error_prompt":"The user's original request was:\n\nShow me how to call a user-defined action from another feature set.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  6:1: error: Duplicate feature set name 'CallAction'\n    hint: A feature set with this name was already defined at line 1\n    hint: Each feature set must have a unique name\n  2:5: warning: Variable 'value' is defined but never used\n  7:5: warning: Variable 'value' is defined but never used\n  Found 1 error(s), 2 warning(s) in main.aro\n\n❌ 1 error(s) found\n⚠️  2 warning(s) found\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block.","attempts":3,"broken_output":"```aro\n(CallAction: Example) {\n    Extract the <value> from the <request: body>.\n    Return the<OK: status> for the <result>.\n}\n\n(CallAction: Example) {\n    Extract the <value> from the <request: body>.\n    Return the<OK: status> for the <result>.\n}\n```","fixed_output":"```aro\n(Add Numbers: Add) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Compute the <sum> as <a> +<b>.\n    Return the<OK: status> with <sum>.\n}\n\n(CallAction: Example) {\n    Extract the <a> from the <request: a>.\n    Extract the <b> from the <request: b>.\n    Return the<OK: status> for the <result>.\n    Return the<OK: status> with the <sum> from the<Add Numbers: Add>.\n}\n```"}
{"attempts":3,"broken_output":"```aro\n(PostToExternal: PostExample) {\n    Extract the <jsonBody> from the <request: body>.\n    Post the <jsonBody> to the <external-url>.\n    Return the<OK: status> for the <result>.\n}\n```","timestamp":"2026-05-23T17:29:15Z","error_prompt":"The user's original request was:\n\nShow me an HTTP client that POSTs JSON to an external endpoint.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  3:15: error: Cannot rebind variable 'jsonBody' - variables are immutable\n    hint: Variable 'jsonBody' was already defined earlier in this feature set\n    hint: Create a new variable with a different name instead\n    hint: Example: <Post> the <jsonBody-updated> to the <external-url>\n  3:33: warning: Variable 'external-url' used before definition\n  3:5: warning: Variable 'jsonBody' is defined but never used\n  Found 1 error(s), 2 warning(s) in main.aro\n\n❌ 1 error(s) found\n⚠️  2 warning(\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block.","fixed_output":"```aro\n(PostToExternal: PostExample) {\n    Extract the <jsonBody> from the <request: body>.\n    Create the <jsonBody-updated> from the <request: body>.\n    Post the <jsonBody-updated> to the <external-url>.\n    Return the<OK: status> for the <result>.\n}\n```"}
{"attempts":3,"timestamp":"2026-05-23T17:33:39Z","fixed_output":"```aro\n(Deduplicate Records: Dedup) {\n    Extract the <records> from the <input>.\n    Sort the <records> by the <id> descending.\n    Deduplicate the <records> by the <id>.\n    Return the<OK: status> with the <records>.\n}\n```","broken_output":"```aro\n(Deduplicate Records: Dedup) {\n    Extract the <records> from the <input>.\n    Sort the <records> by <id> descending.\n    Deduplicate the <records> by <id>.\n    Return the<OK: status> with the <records>.\n}\n```","error_prompt":"The user's original request was:\n\nIdiomatic ARO for: \"deduplicate a list of records by id, keeping the latest\".\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  3:32: error: Expected '.', but got identifier(descending)\n    hint: Statements must end with a period (.)\n  4:22: error: Cannot rebind variable 'records' - variables are immutable\n    hint: Variable 'records' was already defined earlier in this feature set\n    hint: Create a new variable with a different name instead\n    hint: Example: <Deduplicate> the <records-updated> by the <id>\n  4:35: warning: Variable 'id' used before definition\n   warning: External dependency 'input' is not p\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block."}
{"error_prompt":"The user's original request was:\n\nARO snippet that uses the date system object.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  3:17: error: Cannot rebind variable 'date' - variables are immutable\n    hint: Variable 'date' was already defined earlier in this feature set\n    hint: Create a new variable with a different name instead\n    hint: Example: <Format> the <date-updated> into the <iso8601>\n  3:33: warning: Variable 'iso8601' used before definition\n   warning: External dependency 'date' is not published by any feature set\n    hint: Consider adding a <Publish> statement or marking it as framework-provided\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block.","fixed_output":"```aro\n(Format Date: FormatDate) {\n    Create the <current-time> from the <date: now>.\n    Format the <current-time> into the <iso8601>.\n    Return the<OK: status> with the <iso8601>.\n}\n```","broken_output":"```aro\n(Format Date: FormatDate) {\n    Create the <date> from the <date: now>.\n    Format the <date> into the <iso8601>.\n    Return the<OK: status> with the <iso8601>.\n}\n```","timestamp":"2026-05-23T17:34:48Z","attempts":3}
{"timestamp":"2026-05-23T17:36:08Z","attempts":3,"broken_output":"```aro\n(Parallel Process: ParallelExample) {\n    Create the <items> from the <collection>.\n    For each <item> in the <items> {\n        Process the <item>.\n    }\n    Return the<OK: status> for the <process>.\n}\n```","fixed_output":"```aro\n(Process Items: ParallelExample) {\n    Create the <items> from the <collection>.\n    For each <item> in the <items> {\n        Process the <item>.\n    }\n    Return the<OK: status> for the <process>.\n}\n```","error_prompt":"The user's original request was:\n\nARO snippet that uses parallel for-each with a concurrency limit.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  1:2: error: Missing feature set name\n  Found 1 error(s) in main.aro\n\n❌ 1 error(s) found\n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block."}
{"broken_output":"```aro\n(Intersect Sets: SetIntersection) {\n    Create the <setA> from the <collectionA>.\n    Create the <setB> from the <collectionB>.\n    Intersect the <setA> with the <setB>.\n    Return the<OK: status> with the <intersection>.\n}\n```","attempts":3,"error_prompt":"The user's original request was:\n\nARO snippet that intersects two sets.\n\n`aro check` found errors in the ARO code you produced:\n\n```\nmain.aro:\n  4:20: error: Cannot rebind variable 'setA' - variables are immutable\n    hint: Variable 'setA' was already defined earlier in this feature set\n    hint: Create a new variable with a different name instead\n    hint: Example: <Intersect> the <setA-updated> with the <setB>\n  2:33: warning: Variable 'collectionA' used before definition\n  3:33: warning: Variable 'collectionB' used before definition\n  4:5: warning: Variable 'setA' is defined but never used\n  Found 1 error(s), 3 warning(s) \n```\n\nFix the syntax errors WITHOUT changing what the code does.\nEvery action the user asked for must remain in the program —\ndo not delete a Compute, Extract, Log, Emit or Return just to\nmake `aro check` pass. If \"add two numbers\" was the request,\nthe addition step must still be there. If the existing approach\ncannot be made syntactically valid while doing what the user\nasked, rewrite using a different action that achieves the same\nbehaviour — but do not silently drop the behaviour itself.\n\nOutput the corrected code as a complete ARO feature set wrapped\nin `(name: activity) { ... }` inside ```aro fences. Do not write\ntool-call syntax inside the code block.","timestamp":"2026-05-23T17:36:34Z","fixed_output":"```aro\n(Intersect Sets: SetIntersection) {\n    Create the <setA> from the <collectionA>.\n    Create the <setB> from the <collectionB>.\n    Create the <setA-updated> from the <setA>.\n    Intersect the <setA-updated> with the <setB>.\n    Return the<OK: status> with the <intersection>.\n}\n```"}
