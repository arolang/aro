# Test hints for EventSchema
# Demonstrates ARO-0046 typed event extraction with OpenAPI schemas
mode: both
# Event handlers run asynchronously in binary mode, so output order varies
occurrence-check: true
