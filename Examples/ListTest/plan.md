# Build a simple directory listing test

Create a single-file ARO application that lists the current directory. Create a path variable ".", use `List the <entries> from the <directory: path>` to get the listing, compute the entry count with the length qualifier, and log "Found N entries".
