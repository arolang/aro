(Application-Start: List Test) {
    (* List the current directory to be self-contained *)
    <Create> the <path> with ".".
    <Log> "Listing directory..." to the <console>.

    <List> the <entries> from the <directory: path>.

    <Compute> the <count: length> from the <entries>.
    <Log> "Found ${count} entries" to the <console>.

    <Return> an <OK: status> for the <test>.
}
