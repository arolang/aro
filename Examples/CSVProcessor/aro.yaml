# ARO Application Configuration
# Demonstrates using a managed Rust plugin for CSV processing

plugins:
  # Managed plugin from GitHub (installed via aro add)
  - source: https://github.com/arolang/plugin-rust-csv
