(* HTTP API endpoints *)

(* Health check endpoint *)
(healthCheck: WebSocket Demo API) {
    Create the <response> with {
        status: "healthy",
        websocket: "ws://localhost:8080/ws"
    }.
    Return an <OK: status> with <response>.
}
