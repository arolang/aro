# TerminalTaskManager uses Keepalive for event loop
# Timeout is expected behavior - application stays alive until Ctrl+C
timeout: 3
mode: both
# Binary mode outputs return value fields that interpreter doesn't
occurrence-check: true
