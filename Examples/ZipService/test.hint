# Test hints for ZipService
# Package plugin compilation requires Swift compiler at runtime
# Works on arm64 Linux (Docker) but fails on x86_64 Linux (CI)
# The Swift plugin compilation produces different output between architectures
skip-on-linux: ZipPlugin test fails on x86_64 CI (passes on arm64)
mode: both
timeout: 120
# Use occurrence-check for order-independent matching (dictionary output order varies)
# Also needed for binary mode compatibility (plugin loading messages differ)
occurrence-check: true
