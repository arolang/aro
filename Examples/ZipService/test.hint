# Test hints for ZipService
# Plugin tests require Swift compiler (available on macOS and Linux CI)
# Native compilation doesn't support dynamic plugins yet
# Dictionary key order is not guaranteed, use occurrence-check
mode: interpreter
occurrence-check: true
skip-on-windows: Plugin requires Swift compiler (not available on Windows CI)
