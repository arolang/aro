# SystemMonitor is an HTTP API that uses Unix shell commands (ls, df, ps)
# These commands are not available on Windows
type: http
mode: both
skip-on-windows: Uses Unix shell commands (ls, df, ps)
# Output contains dynamic system info (processes, disk usage, directory listings)
# Use occurrence-check to verify response structure without exact matching
occurrence-check: true
