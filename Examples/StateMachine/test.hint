# Test hints for StateMachine example
# Demonstrates Accept action for state transitions with StateTransition Handler observers
# Binary mode: handlers fire asynchronously (occurrence-check handles ordering)
mode: both
occurrence-check: true
timeout: 30
