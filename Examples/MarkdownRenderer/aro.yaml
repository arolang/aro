# ARO Application Configuration
# Demonstrates using a managed Python plugin for Markdown processing

plugins:
  # Managed plugin from GitHub (installed via aro add)
  - source: https://github.com/arolang/plugin-python-markdown
