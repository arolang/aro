# TerminalSystemMonitor uses Keepalive for event loop
# keep-alive sends SIGINT and uses pump loop to capture output before timeout
timeout: 5
mode: both
keep-alive: true
occurrence-check: true
allow-error: true
