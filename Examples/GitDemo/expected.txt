# Type: console
# Command: aro run ./Examples/GitDemo
# NOTE: hand-maintained, not `--generate`d. The whole body of this example's
# output is a property of the checkout it runs in — the branch name, the clean
# flag, and every line of the commit log, including multi-line commit bodies.
# Regenerating would bake one machine's branch and history into the contract.
# Only the structural markers are asserted, plus the trailing status; test.hint
# sets occurrence-check, so the varying lines are extra output, and `Branch:`
# and `Clean:` match the start of the real lines (GitLab #820).
---
=== ARO Git Actions Demo ===
--- Repository Status ---
Branch:
Clean:
--- Recent Commits ---
=== Demo complete ===
[OK] startup
