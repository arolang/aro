# Build a simple weather API client

Create a single-file ARO application that fetches weather data from an external API. In the `Application-Start` feature set, log "Fetching weather...", create an API URL for a weather forecast endpoint, use `Request the <response> from the <api-url>` to fetch the data, extract the body from the response, log it, and return OK.
