# ARO Application Configuration
# This file configures custom plugins for the application

plugins:
  # Managed Swift plugin (compiled automatically by aro build/run)
  - source: Plugins/GreetingService
