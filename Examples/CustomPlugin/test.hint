# Test hints for CustomPlugin
# Binary mode: plugins are compiled at build time by 'aro build'
# Plugin requires Swift compiler at build time (available in CI via swift:6.2-jammy)
mode: compiled
timeout: 60
# Use occurrence-check since binary output has extra logging lines
occurrence-check: true
