# Build a minimal hello world application

Create the simplest possible ARO application. It should have a single `main.aro` file with an `Application-Start` feature set that creates a greeting string "Hello, ARO World!", logs it to the console, and returns an OK status.
