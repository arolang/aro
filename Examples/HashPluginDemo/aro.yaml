# ARO Application Configuration
# Demonstrates using a managed C plugin for hash functions

plugins:
  # Managed plugin from GitHub (installed via aro add)
  - source: https://github.com/arolang/plugin-c-hash
