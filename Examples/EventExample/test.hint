# Skip due to TTY detection changes
skip: TTY detection affects console output

# EventExample has concurrent event handlers with non-deterministic order
# Use occurrence-based validation instead of exact output matching
occurrence-check: true
