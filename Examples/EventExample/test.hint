# Test hints for EventExample
# EventExample has concurrent event handlers with non-deterministic order
# Event handler execution order varies between platforms
# Skip this test as output is inherently non-deterministic
skip: Concurrent event handlers have non-deterministic output order
