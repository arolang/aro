# Test hints for SQLiteExample
# Package plugin compilation requires Swift compiler - available on CI via swift:6.2-jammy
# Requires libsqlite3-dev on Linux (not installed in CI)
skip-on-linux: Missing libsqlite3-dev dependency in CI environment
mode: interpreter
timeout: 120
