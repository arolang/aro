# Test hints for SQLiteExample
# Package plugin compilation requires Swift compiler at runtime
# Skipped on Linux: CI runner cannot access GitHub to fetch Swift package dependencies
# Error: "Failed to clone repository https://github.com/stephencelis/SQLite.swift.git"
skip-on-linux: CI runner (spencer) cannot fetch GitHub dependencies
# Swift package plugins don't work on Windows (different plugin loading mechanism)
skip-on-windows: Swift package plugins not supported
mode: interpreter
timeout: 120
# Use occurrence-check for cross-platform compatibility (dictionary ordering varies)
occurrence-check: true
