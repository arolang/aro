# Test hints for SQLiteExample
# Package plugin compilation requires Swift compiler at runtime
# Skipped on Linux: SQLite.swift package compilation fails silently in CI environment
# TODO: Investigate Linux plugin build issues (possibly missing Swift package dependencies)
skip-on-linux: SQLite.swift package compilation fails in CI
mode: interpreter
timeout: 120
# Use occurrence-check for cross-platform compatibility (dictionary ordering varies)
occurrence-check: true
