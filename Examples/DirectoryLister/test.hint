# Test hints for DirectoryLister
# Directory listing output varies significantly by environment (different files, users, permissions)
# Use occurrence-check to only verify fixed header lines appear
skip-on-windows: Uses Unix shell commands (ls)
occurrence-check: true
