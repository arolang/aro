# Test hints for UserService
# Binary mode works with HTTP body reading and error handling fixes
mode: both
# HTTP servers hang on Windows CI
skip-on-windows: HTTP server tests not supported on Windows CI
# JSON field order may vary between platforms
occurrence-check: true
