# Test hints for HTTPClient
# Uses occurrence-check since API responses contain dynamic data (weather, timestamps)
# Verifies HTTP client functionality and response structure
# External API calls can fail due to network restrictions/timeouts in CI environments
occurrence-check: true
skip-on-windows: Windows SSL and network stack differences cause output variations
skip-on-linux: External API calls unreliable in Linux CI (network restrictions, timeouts)
